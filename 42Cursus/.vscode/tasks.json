{
    "tasks": [
        {
            "type": "shell",
            "label": "printf",
            "command": "/usr/bin/gcc",
            "args": [
                "-g",
				//"${file}",
				"${fileDirname}/*.c",
				"${fileDirname}/libft/*.c",
                "-o",
                "${fileDirname}/a.out"
            ],
            "options": {
                "cwd": "/usr/bin"
            }
        }
    ],
    "version": "2.0.0"
}